{
  "name": "scoreflow",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build --webpack",
    "build:export": "next build --webpack",
    "start": "next start",
    "lint": "next lint",
    "predeploy": "rm -rf public/_next && npm run build && rm -rf public && mkdir -p public && cp .next/server/app/index.html public/index.html && mkdir -p public/_next/static && cp -r .next/static/* public/_next/static/ && cp -r node_modules/pdfjs-dist/build/pdf.worker.min.mjs public/pdf.worker.min.js || true",
    "deploy": "npm run predeploy && firebase deploy",
    "deploy:hosting": "npm run predeploy && firebase deploy --only hosting",
    "deploy:firestore": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "firebase": "^10.12.2",
    "next": "^16.0.7",
    "pdfjs-dist": "^4.0.379",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-firebase-hooks": "^5.1.1",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "eslint": "^8.57.0",
    "eslint-config-next": "14.2.5",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.5.4"
  }
}
